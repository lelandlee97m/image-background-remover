# image-background-remover

基于 Remove.bg API + Cloudflare Workers 的在线图像背景去除工具。

## 目录结构

```
project/
├── frontend/        # 前端页面（React/Vue 或纯JS）
├── worker/          # Cloudflare Workers 后端逻辑
└── image-bg-remover-mvp.md  # MVP 需求文档
```
